import React from "react";
import PageTitle from '../PageTitle';

export default function RewardsPage() {
    return(
        <div>
            <PageTitle 
                title={'Add your rewards'}
                subtitle={'Offer simple, meaningful rewards that bring backers closer to your project. Rewards don’t have to be physical items. Consider special experiences or behind-the-scenes peeks into your project.'}/>
        </div>
    );
}